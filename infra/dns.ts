export const domain = $app.stage === 'production' ? 'pandoks.com' : 'dev.pandoks.com';

export const EXAMPLE_DOMAIN = 'example.pandoks.com';

export const STAGE_NAME = $app.stage === 'production' ? 'prod' : 'dev';
