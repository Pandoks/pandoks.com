"""apex-browser -- the best-of-both-worlds stealth headless Chrome service.

Core stealth (real Chrome + nodriver + coherent identity) is stealth-browser's
empirically-proven winner, reused verbatim. The HTTP service + SessionManager
design is headless-stealth's productized API, ported to Python.
"""
