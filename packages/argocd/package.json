{
  "name": "@pandoks.com/argocd",
  "version": "0.0.1",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "build": "pnpm docker:build",
    "docker:build": "docker build -f ./Dockerfile ../../ -t localhost:12345/argocd:latest",
    "dev:push": "docker push localhost:12345/argocd:latest"
  }
}
