diff --git a/src/exports/vite/index.js b/src/exports/vite/index.js
index fe829e73e240d283f9f7557ffc60ebfe6973b1b6..99896902259855407ee3dd15d6e1de728378b7aa 100644
--- a/src/exports/vite/index.js
+++ b/src/exports/vite/index.js
@@ -385,6 +385,7 @@ async function kit({ svelte_config }) {
 						__SVELTEKIT_ADAPTER_NAME__: s(kit.adapter?.name),
 						__SVELTEKIT_APP_VERSION_FILE__: s(`${kit.appDir}/version.json`),
 						__SVELTEKIT_APP_VERSION_POLL_INTERVAL__: s(kit.version.pollInterval),
+						__SVELTEKIT_PRELOAD_DATA_PERSIST__: s(!!kit.adapter?.preloadDataPersist),
 						__SVELTEKIT_PAYLOAD__: new_config.build.ssr
 							? '{}'
 							: `globalThis.__sveltekit_${version_hash}`
diff --git a/src/runtime/client/client.js b/src/runtime/client/client.js
index c52354928a50f15235d3854242749704997be653..92fb124b5a9469bf0abf448cc066f629b61b9f2f 100644
--- a/src/runtime/client/client.js
+++ b/src/runtime/client/client.js
@@ -222,12 +222,22 @@ const invalidated = [];
  */
 const components = [];
 
-/** @type {{id: string, token: {}, promise: Promise<import('./types.js').NavigationResult>, fork: Promise<import('svelte').Fork | null> | null} | null} */
-let load_cache = null;
-
-function discard_load_cache() {
-	void load_cache?.fork?.then((f) => f?.discard());
-	load_cache = null;
+/** @type {Map<string, {id: string, token: {}, promise: Promise<import('./types.js').NavigationResult>, fork: Promise<import('svelte').Fork | null> | null}>} */
+let load_cache = new Map();
+
+function discard_load_cache(id) {
+	if (id) {
+		const entry = load_cache.get(id);
+		if (entry) {
+			void entry.fork?.then((f) => f?.discard());
+			load_cache.delete(id);
+		}
+	} else {
+		for (const entry of load_cache.values()) {
+			void entry.fork?.then((f) => f?.discard());
+		}
+		load_cache.clear();
+	}
 }
 
 /**
@@ -540,36 +550,35 @@ export async function _goto(url, options, redirect_count, nav_token) {
 
 /** @param {import('./types.js').NavigationIntent} intent */
 async function _preload_data(intent) {
-	// Reuse the existing pending preload if it's for the same navigation.
-	// Prevents an edge case where same preload is triggered multiple times,
-	// then a later one is becoming the real navigation and the preload tokens
-	// get out of sync.
-	if (intent.id !== load_cache?.id) {
-		discard_load_cache();
-
+	// Reuse pending or unconsumed preloads to avoid duplicate fetches.
+	// Adapters that produce immutable output can opt out of consumed-entry refreshing
+	// by setting `preloadDataPersist: true`, which exposes __SVELTEKIT_PRELOAD_DATA_PERSIST__.
+	const existing = load_cache.get(intent.id);
+	const should_refresh = existing?.consumed && !__SVELTEKIT_PRELOAD_DATA_PERSIST__;
+	if (!existing || should_refresh) {
+		if (existing) discard_load_cache(intent.id);
 		const preload = {};
 		preload_tokens.add(preload);
-		load_cache = {
+		const entry = {
 			id: intent.id,
 			token: preload,
 			promise: load_route({ ...intent, preload }).then((result) => {
 				preload_tokens.delete(preload);
 				if (result.type === 'loaded' && result.state.error) {
 					// Don't cache errors, because they might be transient
-					discard_load_cache();
+					discard_load_cache(intent.id);
 				}
 				return result;
 			}),
 			fork: null
 		};
+		load_cache.set(intent.id, entry);
 
 		if (__SVELTEKIT_FORK_PRELOADS__ && svelte.fork) {
-			const lc = load_cache;
-
-			lc.fork = lc.promise.then((result) => {
-				// if load_cache was discarded before load_cache.promise could
+			entry.fork = entry.promise.then((result) => {
+				// if load_cache was discarded before promise could
 				// resolve, bail rather than creating an orphan fork
-				if (lc === load_cache && result.type === 'loaded') {
+				if (load_cache.get(intent.id) === entry && result.type === 'loaded') {
 					try {
 						return svelte.fork(() => {
 							root.$set(result.props);
@@ -585,7 +594,7 @@ async function _preload_data(intent) {
 		}
 	}
 
-	return load_cache.promise;
+	return load_cache.get(intent.id).promise;
 }
 
 /**
@@ -1104,10 +1113,11 @@ function preload_error({ error, url, route, params }) {
  * @returns {Promise<import('./types.js').NavigationResult>}
  */
 async function load_route({ id, invalidating, url, params, route, preload }) {
-	if (load_cache?.id === id) {
+	if (load_cache.has(id)) {
 		// the preload becomes the real navigation
-		preload_tokens.delete(load_cache.token);
-		return load_cache.promise;
+		const cached = load_cache.get(id);
+		preload_tokens.delete(cached.token);
+		return cached.promise;
 	}
 
 	const { errors, layouts, leaf } = route;
@@ -1784,13 +1794,13 @@ async function navigate({
 	}
 
 	// also compare ids to avoid using wrong fork (e.g. a new one could've been added while navigating)
-	const load_cache_fork = intent && load_cache?.id === intent.id ? load_cache.fork : null;
-	// reset preload synchronously after the history state has been set to avoid race conditions
-	if (load_cache?.fork && !load_cache_fork) {
-		// discard fork of different route
-		discard_load_cache();
+	const cached_entry = intent ? load_cache.get(intent.id) : null;
+	const load_cache_fork = cached_entry?.fork ?? null;
+	// consume fork and mark entry so subsequent preloads refresh it
+	if (cached_entry) {
+		cached_entry.fork = null;
+		cached_entry.consumed = true;
 	}
-	load_cache = null;
 
 	navigation_result.props.page.state = state;
 
diff --git a/src/types/global-private.d.ts b/src/types/global-private.d.ts
index 4b442f89944ca20521064e4c519b45dee00f07ec..2e1ca12ab19b8b5cb38650ee29e4beb4895bfe64 100644
--- a/src/types/global-private.d.ts
+++ b/src/types/global-private.d.ts
@@ -11,6 +11,8 @@ declare global {
 	const __SVELTEKIT_SERVER_TRACING_ENABLED__: boolean;
 	/** True if `config.kit.experimental.forkPreloads` is `true` */
 	const __SVELTEKIT_FORK_PRELOADS__: boolean;
+	/** True if the active adapter sets `preloadDataPersist: true` to keep preload cache entries across navigations */
+	const __SVELTEKIT_PRELOAD_DATA_PERSIST__: boolean;
 	/** True if `config.kit.router.resolution === 'client'` */
 	const __SVELTEKIT_CLIENT_ROUTING__: boolean;
 	/** True if `config.kit.router.type === 'hash'` */
