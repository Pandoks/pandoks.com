name: Cluster Tests

permissions:
  contents: read

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: cluster-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      postgres: ${{ steps.filter.outputs.postgres }}
      valkey: ${{ steps.filter.outputs.valkey }}
      clickhouse: ${{ steps.filter.outputs.clickhouse }}
      harness: ${{ steps.filter.outputs.harness }}

    steps:
      - uses: actions/checkout@v6

      - id: filter
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4
        with:
          filters: |
            postgres:
              - 'packages/postgres/**'
            valkey:
              - 'packages/valkey/**'
            clickhouse:
              - 'packages/clickhouse/**'
            harness:
              - 'packages/testkit/**'
              - 'scripts/cluster/**'
              - 'scripts/lib/**'
              - 'docker-compose.yaml'
              - '.github/workflows/cluster-tests.yaml'

  cluster-tests:
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.postgres == 'true' ||
      needs.detect-changes.outputs.valkey == 'true' ||
      needs.detect-changes.outputs.clickhouse == 'true' ||
      needs.detect-changes.outputs.harness == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup-node-pnpm

      - uses: ./.github/actions/setup-mise
        with:
          tools: k3d kubectl helm

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: packages/testkit/go.mod
          cache-dependency-path: |
            packages/testkit/go.sum
            packages/postgres/test/go.sum
            packages/valkey/test/go.sum
            packages/clickhouse/test/go.sum

      - name: Start dependencies and k3d cluster
        run: |
          pnpm cluster k3d deps up
          pnpm cluster k3d up

      - name: Build and push database images
        run: |
          pnpm --filter @pandoks.com/postgres --filter @pandoks.com/valkey --filter @pandoks.com/clickhouse run docker:build
          pnpm --filter @pandoks.com/postgres --filter @pandoks.com/valkey --filter @pandoks.com/clickhouse run dev:push

      - name: Run cluster tests in parallel
        run: ./scripts/cluster/main.sh test all --parallel

  cluster-tests-pass:
    needs:
      - cluster-tests
    if: ${{ always() && !cancelled() && !contains(needs.*.result, 'failure') }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "ok"
