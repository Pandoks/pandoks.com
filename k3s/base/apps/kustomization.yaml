apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # App manifests live with their projects under apps/<project>/kube.
  # NOTE: render with kubectl kustomize --load-restrictor LoadRestrictionsNone
  - ../../../apps/example/kube
